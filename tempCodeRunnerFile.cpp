#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, k;
    cin >> n >> k;

    int sum = 0;
    int remaining;
    int ans;
    while (remaining > 0)
    {
        for (int i = 1; i <= n; i++)
        {
            ans = i;
            sum = sum + n * 5
        }
        remaining = 240 - sum + k;
    }

    cout << ans << endl;

    return 0;
}